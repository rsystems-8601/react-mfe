function calculateCurr(num) {
  let total = num;
   sum = (input) => {
    if (input) {
      total += input;
      return this.sum; 
    } 
    return total;
  }
  return this.sum;
}

const resultCurr = calculateCurr(1)(2)(3)(4);
resultCurr();
// Creation phase:
// ngOnChanges: Invoked before the first change detection after binding values to inputs.
// ngOnInit: Called after the first change detection and component initialization.
// •	Change detection phase:
// ngDoCheck: Executed during every change detection cycle, allowing custom checks.
// •	View initialization phase:
// ngAfterViewInit: Runs after the component's view has been fully initialized.
// •	Destruction phase:
// ngOnDestroy: Called just before the component is destroyed and removed from the DOM.
